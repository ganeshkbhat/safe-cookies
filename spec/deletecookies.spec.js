
const { describe, beforeAll, afterAll,  expect, jasmine} = require('jasmine');
const { getCookie, setCookie, deleteCookie } = require("../index")

describe('[ tests] Tests to ', () => {
  let tst, written;
  beforeAll(() => { });

  afterAll(() => { });

  it('should ', () => {
    let actual = true;
    let expected = true;
    // expect(actual).not.toBe(expected);
    expect(actual).toBe(expected);

  });

  it('should ', () => {
    let actual = true;
    let expected = true;
    // expect(actual).not.toBe(expected);
    expect(actual).toBe(expected);
  });

});
