/**
 * 
 * Package: safecookie
 * Author: Ganesh B
 * Description: 
 * Install: npm i safecookie --save
 * Github: https://github.com/ganeshkbhat/
 * npmjs Link: https://www.npmjs.com/package/
 * File: test/index.test.js
 * File Description: 
 * 
*/

/* eslint no-console: 0 */

'use strict';

const expect = require('chai').expect;

describe('[cookies tests] Tests to ', () => {
  let tst, written;
  before(() => {
    
  });

  after(() => { });

  it('should be able to - blanktest', () => {
    let actual = true;
    let expected = true;
    expect(actual).to.equal(expected);
  });

  it('should be able to - blanktest', () => {
    let actual = true;
    let expected = true;
    expect(actual).to.equal(expected);
  });

});
